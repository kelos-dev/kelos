#!/usr/bin/env bash
# Existing command name for older git config. Credential minting is handled by
# cody-tools; this script does not sign GitHub App JWTs locally.
set -euo pipefail

exec /usr/local/bin/cody-github-credential-helper "$@"

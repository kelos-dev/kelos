apiVersion: kelos.dev/v1alpha1
kind: Workspace
metadata:
  name: my-project
spec:
  repo: https://github.com/owner/repo.git # TODO: replace with your repository URL
  secretRef:
    name: github-token

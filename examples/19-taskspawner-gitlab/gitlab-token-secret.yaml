apiVersion: v1
kind: Secret
metadata:
  name: gitlab-token
type: Opaque
stringData:
  # TODO: Replace with a GitLab personal, group, or project access token.
  # Required scope: api (clone, read issues/merge requests, post notes).
  GITLAB_TOKEN: "glpat-REPLACE-ME"

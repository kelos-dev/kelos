# Only needed for taskspawner-webhook.yaml. Referenced by the Helm value
# webhookServer.sources.gitlab.secretName and entered as the "Secret token" of
# the GitLab project webhook.
apiVersion: v1
kind: Secret
metadata:
  name: gitlab-webhook-secret
type: Opaque
stringData:
  # TODO: Replace with a random token
  WEBHOOK_SECRET: "REPLACE-ME"
